import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF62CD5D);
  static const lightBackground = Color(
    0xFFF2F2F2,
  ); // Corrected hex code (0xAARRGGBB)
  static const darkBackground = Color(0xFF070707);
  static const grey = Color(0xFFEBEBEB); // Corrected hex code (0xAARRGGBB)
  static const darkGrey = Color(0xFF737373);
}
